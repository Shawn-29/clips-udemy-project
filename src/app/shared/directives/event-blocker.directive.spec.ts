import { SpudDirective } from './event-blocker.directive';

describe('SpudDirective', () => {
  it('should create an instance', () => {
    const directive = new SpudDirective();
    expect(directive).toBeTruthy();
  });
});
